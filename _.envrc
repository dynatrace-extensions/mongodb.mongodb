# if you'd like to edit this scripts please bare in mind that
# it's .gitignored, so the best way to edit it is to clone
# a fresh repo, do the edits there and commit
PATH_add scripts
make __dt_internal_hook 2>/dev/null || true
PATH_add dynatrace-internal/bin
use_nix